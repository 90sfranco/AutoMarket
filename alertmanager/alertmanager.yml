global:
  resolve_timeout: 10m

route:
  receiver: discord_webhook
  repeat_interval: 30s

receivers:
  - name: discord_webhook
    webhook_configs:
      - url: "http://discord-proxy:9600/"
        send_resolved: true
