global:
  scrape_interval: 5s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "kafka_exporter"
    static_configs:
      - targets: ["kafka-exporter:3000"]

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]
